<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" >

    <TextView
        android:id="@+id/tv_marker_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="4dp"
        android:background="@drawable/et_multi_row"
        android:padding="4dp"
        android:gravity="center"
        android:visibility="gone"
        android:textSize="@dimen/text_size_small"  />

    <RelativeLayout
        android:layout_below="@+id/tv_marker_title"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content">

        <ImageView
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/ic_marker_gennext" />

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/iv_marker_client"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="6dp" />

        <ImageView
            android:layout_width="60dp"
            android:layout_height="50dp"
            android:layout_below="@+id/iv_marker_client"
            android:layout_marginTop="@dimen/vertical_margin_half"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_marker_client"
            android:visibility="gone" />
    </RelativeLayout>
</RelativeLayout>